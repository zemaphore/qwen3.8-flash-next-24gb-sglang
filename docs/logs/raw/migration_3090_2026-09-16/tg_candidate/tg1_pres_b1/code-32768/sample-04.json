{
 "started_utc": "2026-09-16T23:23:36.298512+00:00",
 "wall_seconds": 29.282922127997153,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:24:05.581761+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 105,
  "temperature_c": {
   "min": 77.0,
   "max": 79.0,
   "mean": 78.77142857142857
  },
  "power_w": {
   "min": 246.29,
   "max": 279.82,
   "mean": 265.7
  },
  "util_pct": {
   "min": 57.0,
   "max": 99.0,
   "mean": 93.25714285714285
  },
  "clock_sm_mhz": {
   "min": 1410.0,
   "max": 1905.0,
   "mean": 1744.5714285714287
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1526.0
  }
 },
 "output_text_sha256": "9467c2cf2574f4c715335f8abb96b604ab5663a8c45d87ff0690ff73a6fa6c7a",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free - step_bytes < reserve:\n+                break\n+            try:\n+                self.set_S(min(self.E, min(st.S for st in self.layers.values()) + step))\n+            except ArenaOOM:\n+                break\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            step_bytes = sum(sum(k.rb for k in st.kinds.values()) for st in self.layers.values()) * step\n+            if free + step_bytes > reserve:\n+                break\n+            self.set_S(max(self.s_floor(), min(st.S for st in self.layers.values()) - step))\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", min(st.S for st in self.layers.values()),\n+                    torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n",
 "ttft_seconds": 13.19860749700456,
 "status": "ok",
 "tg_tokens_per_s": 31.77028157671766,
 "decode_seconds_per_token": 0.031475956471623906,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 31.17732900136616,
  "p95": 41.31399300240446,
  "mean": 31.475956471623906
 },
 "class": "code",
 "requested_input_tokens": 32768
}
