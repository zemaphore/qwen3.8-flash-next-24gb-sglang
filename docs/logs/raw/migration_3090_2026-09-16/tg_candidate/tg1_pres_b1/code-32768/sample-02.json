{
 "started_utc": "2026-09-16T23:22:37.895849+00:00",
 "wall_seconds": 29.21151504998852,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:23:07.107468+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 105,
  "temperature_c": {
   "min": 77.0,
   "max": 79.0,
   "mean": 78.48571428571428
  },
  "power_w": {
   "min": 245.28,
   "max": 279.84,
   "mean": 265.783619047619
  },
  "util_pct": {
   "min": 84.0,
   "max": 99.0,
   "mean": 93.83809523809524
  },
  "clock_sm_mhz": {
   "min": 1050.0,
   "max": 1905.0,
   "mean": 1722.5714285714287
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1448.0
  }
 },
 "output_text_sha256": "41d5e07f44e17160d2034ba37a3299eb0e5ef918a40f9c9d5b9b796dd57f38a0",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    #+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ----------------------------------------------------------------+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the Mo+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (pref+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls +=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve);+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl)\n+        except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl)\n+        except OSError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl)\n+        except OSError:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl)\n+        except OSError:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl)\n+        except OSError:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl)\n+        except OSError:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl)\n+        except OSError:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl)\n+        except OSError:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl)\n+        except OSError:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * max(1, self.E - S) * sum(k.rb for st in self.layers.values() for k in st.kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            self.set_S(S)\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        return max(st.S for st in self.layers.values())\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] < self._autofill << 20:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.set_S(self.regrow_reserve); self.pending_regrow = False\n+        if not self.ctl:\n+            return\n+        try:\n+            st = os.stat(self.ctl)\n+        except OSError:\n+            return\n+       ",
 "ttft_seconds": 13.218212996987859,
 "status": "ok",
 "tg_tokens_per_s": 31.951004918951284,
 "decode_seconds_per_token": 0.03129792012916828,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 31.008207006379962,
  "p95": 39.769429989974014,
  "mean": 31.29792012916828
 },
 "class": "code",
 "requested_input_tokens": 32768
}
