{
 "started_utc": "2026-09-16T23:23:07.110706+00:00",
 "wall_seconds": 29.184491322012036,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:23:36.295282+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 104,
  "temperature_c": {
   "min": 77.0,
   "max": 79.0,
   "mean": 78.77884615384616
  },
  "power_w": {
   "min": 245.28,
   "max": 279.91,
   "mean": 266.29076923076923
  },
  "util_pct": {
   "min": 74.0,
   "max": 100.0,
   "mean": 94.28846153846153
  },
  "clock_sm_mhz": {
   "min": 1395.0,
   "max": 1905.0,
   "mean": 1749.375
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1526.0
  }
 },
 "output_text_sha256": "b62cbd34202d76287583a095ac1329fff79b6afc043d1859aca6df5e02f9741d",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until <+        \"\"\"Grow every layer until <reserve+        \"\"\"Grow every layer until <reserve_mb+        \"\"\"Grow every layer until <reserve_mb>+        \"\"\"Grow every layer until <reserve_mb> of+        \"\"\"Grow every layer until <reserve_mb> of VR+        \"\"\"Grow every layer until <reserve_mb> of VRAM+        \"\"\"Grow every layer until <reserve_mb> of VRAM is+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <=+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill:+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\",+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free /+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**3+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S ++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step,+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+               +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <=+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+               +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d:+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\",+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S,+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free /+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**3+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30,+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time()+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() -+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+   +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self,+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb,+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >=+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VR+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb)+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) <<+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 2+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >=+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free:+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\",+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free /+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**3+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S -+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step,+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >=+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next ==+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+               +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+           +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d:+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\",+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S,+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free /+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**3+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30,+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time()+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() -+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_reg+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow =+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+   +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _reg+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n++        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+       +        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink,+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve_rows+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve_rows pool+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve_rows pool absorbs+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve_rows pool absorbs the+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve_rows pool absorbs the rows+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve_rows pool absorbs the rows).+        \"\"\"Grow every layer until <reserve_mb> of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            logger.info(\"elastic fill: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink every layer until >= target_mb of VRAM is free.\"\"\"\n+        target = int(target_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= target:\n+            logger.info(\"elastic free: already at %.2f GB free\", free / 2**30)\n+            return\n+        t0 = time.time()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target or S_next == self.s_floor():\n+                break\n+            S = S_next\n+        logger.info(\"elastic free to S=%d: %.2f GB free in %.2fs\", S, free / 2**30, time.time() - t0)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve_rows pool absorbs the rows).\"\"\"",
 "ttft_seconds": 13.290010356999119,
 "status": "ok",
 "tg_tokens_per_s": 32.149646521505005,
 "decode_seconds_per_token": 0.031104541050897612,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 31.110523996176198,
  "p95": 38.62292200210504,
  "mean": 31.104541050897613
 },
 "class": "code",
 "requested_input_tokens": 32768
}
