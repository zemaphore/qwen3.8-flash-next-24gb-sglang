{
 "started_utc": "2026-09-16T23:21:33.816714+00:00",
 "wall_seconds": 34.7854844800022,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:22:08.602334+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 125,
  "temperature_c": {
   "min": 74.0,
   "max": 78.0,
   "mean": 77.224
  },
  "power_w": {
   "min": 167.94,
   "max": 281.46,
   "mean": 249.44
  },
  "util_pct": {
   "min": 0.0,
   "max": 99.0,
   "mean": 76.776
  },
  "clock_sm_mhz": {
   "min": 1215.0,
   "max": 1905.0,
   "mean": 1778.52
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1390.0
  }
 },
 "output_text_sha256": "b306122776c27c784318afa3001e9c880e0740fea4843cb43941283d619508d5",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L{+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L{lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L{lid}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L{lid} S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_next = max(S - step, 0)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def _regrow(self, reserve_mb):\n+        \"\"\"Grow back to the pre-shrink reserve after a forced shrink.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.regrow_reserve:\n+            S_next = min(S + 8, self.regrow_reserve)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L{lid} S={",
 "ttft_seconds": 18.296571585000493,
 "status": "ok",
 "tg_tokens_per_s": 30.990649776199223,
 "decode_seconds_per_token": 0.03226779713305652,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 32.084468999528326,
  "p95": 40.67882400704548,
  "mean": 32.26779713305652
 },
 "class": "code",
 "requested_input_tokens": 32768
}
