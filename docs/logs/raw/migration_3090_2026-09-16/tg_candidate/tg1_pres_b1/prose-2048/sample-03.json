{
 "started_utc": "2026-09-16T23:17:45.285667+00:00",
 "wall_seconds": 14.618825840996578,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:17:59.904689+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 52,
  "temperature_c": {
   "min": 74.0,
   "max": 76.0,
   "mean": 75.26923076923077
  },
  "power_w": {
   "min": 255.53,
   "max": 276.73,
   "mean": 267.36115384615385
  },
  "util_pct": {
   "min": 83.0,
   "max": 100.0,
   "mean": 90.84615384615384
  },
  "clock_sm_mhz": {
   "min": 1470.0,
   "max": 1920.0,
   "mean": 1890.2884615384614
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "d468ea7fe1a2099eb0e78e9ec37bf8468207a8cdf4e4b2427520aa84210c0507",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=....py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/b.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ....py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ....py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, M.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin +.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier19.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier19.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier19.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier19.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier19.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier19.py, ngram_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier19.py, ngram_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier19.py, ngram_tier2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier19.py, ngram_tier20.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier19.py, ngram_tier20.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier19.py, ngram_tier20.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier19.py, ngram_tier20.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers on `http://127.0.0.1:30000` after about\n100 seconds. The first request takes roughly 10 minutes: the PLE table loads from disk and the expert arena\nfills. `tools/bench.py` prints `bench: ...` and `tools/ple.py` prints `ple: ...` on success.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, KV cache, elastic expert cache, PLE, MTP, quantization,\n                the Triton configs for sm_120, the sm_120 CUDA kernels, the sm_120a Triton kernels\nkernels/        the CUDA kernels as shipped in the patch, with their build recipe\nassets/         the two serving assets: the PLE n-gram table (ple.f8_e4m3.bin + ple.json) and the\n                accepted state (phase1_state.json)\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_tier.py, ngram_tier2.py,\n                ngram_tier3.py, ngram_tier4.py, ngram_tier5.py, ngram_tier6.py, ngram_tier7.py,\n                ngram_tier8.py, ngram_tier9.py, ngram_tier10.py, ngram_tier11.py, ngram_tier12.py,\n                ngram_tier13.py, ngram_tier14.py, ngram_tier15.py, ngram_tier16.py, ngram_tier17.py,\n                ngram_tier18.py, ngram_tier19.py, ngram_tier20.py, ngram",
 "ttft_seconds": 1.2075115549960174,
 "status": "ok",
 "tg_tokens_per_s": 38.102353727373746,
 "decode_seconds_per_token": 0.02624509780039057,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 25.78027998970356,
  "p95": 33.25807099463418,
  "mean": 26.245097800390567
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
