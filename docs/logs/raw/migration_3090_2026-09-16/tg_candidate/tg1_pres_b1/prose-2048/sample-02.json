{
 "started_utc": "2026-09-16T23:17:30.846987+00:00",
 "wall_seconds": 14.435672271996737,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:17:45.282805+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 52,
  "temperature_c": {
   "min": 74.0,
   "max": 75.0,
   "mean": 74.4423076923077
  },
  "power_w": {
   "min": 252.66,
   "max": 276.47,
   "mean": 267.0261538461539
  },
  "util_pct": {
   "min": 88.0,
   "max": 100.0,
   "mean": 90.57692307692308
  },
  "clock_sm_mhz": {
   "min": 1710.0,
   "max": 1920.0,
   "mean": 1907.3076923076924
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "86b7d28a81c441b2708c32bf451c05ba960fa8b1984c16799b23e1c39f740c6a",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:32.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01...py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py,\n                ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py,\n                ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py,\n                ngram_bench2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py,\n                ngram_bench23.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py,\n                ngram_bench23.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py,\n                ngram_bench23.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py,\n                ngram_bench23.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py,\n                ngram_bench23.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), build.sh (the patch builder), quantize.sh, bench.sh,\n                the tier/elastic/PLE control scripts, the PLE table builder\ntools/          the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,\n                ngram_bench3.py, ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,\n                ngram_bench8.py, ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,\n                ngram_bench13.py, ngram_bench14.py, ngram_bench15.py, ngram_bench16.py, ngram_bench17.py,\n                ngram_bench18.py, ngram_bench19.py, ngram_bench20.py, ngram_bench21.py, ngram_bench22.py,\n                ngram_bench23.py, ngram_b",
 "ttft_seconds": 1.1565335009945557,
 "status": "ok",
 "tg_tokens_per_s": 38.48166934773618,
 "decode_seconds_per_token": 0.02598639863992357,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 25.75111700571142,
  "p95": 32.336727002984844,
  "mean": 25.98639863992357
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
