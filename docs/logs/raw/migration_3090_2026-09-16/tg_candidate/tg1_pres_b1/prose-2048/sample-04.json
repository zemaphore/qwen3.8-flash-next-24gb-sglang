{
 "started_utc": "2026-09-16T23:17:59.906812+00:00",
 "wall_seconds": 14.752985847007949,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:18:14.659912+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 53,
  "temperature_c": {
   "min": 75.0,
   "max": 77.0,
   "mean": 75.9245283018868
  },
  "power_w": {
   "min": 257.96,
   "max": 276.78,
   "mean": 267.7318867924528
  },
  "util_pct": {
   "min": 87.0,
   "max": 100.0,
   "mean": 90.71698113207547
  },
  "clock_sm_mhz": {
   "min": 1650.0,
   "max": 1920.0,
   "mean": 1898.2075471698113
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "a614a72c792654a0d4ee134847e3174de7f9c0a582183912a277bc24dada0d8c",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --re.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --re.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_E.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBED.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDD.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `k.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert G.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE G.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/C.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,00.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 13.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUT.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `docs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `docs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `docs/DES.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `docs/DESIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `docs/DESIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `docs/DESIGN.md`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `docs/DESIGN.md` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `docs/DESIGN.md` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `docs/DESIGN.md` | the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `docs/DESIGN.md` | the design document.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `docs/DESIGN.md` | the design document |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `docs/DESIGN.md` | the design document |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `docs/DESIGN.md` | the design document |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints the launch banner and the `PLE loaded` line when the table is ready; the first\nrequest after it takes a few seconds of JIT compilation. The banner reports the KV pool size, the expert\ncache floor and the PLE table path. The full launch matrix (including the `--ple-mode` options and the\n`PLE`/`PLE_MODE`/`PLE_MAX_SLOTS`/`PLE_MAX_BYTES`/`PLE_MAX_EMBEDDINGS`/`PLE_MAX_ROWS` variables) is in\n[`docs/README.md`](docs/README.md).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 15 files) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 PLE GEMM, the int4 KV read/write, the int8 ring |\n| `patches/` | the Python patches: the KV cache tiers, the PLE table, the expert cache, the elastic controller |\n| `assets/` | the two serving assets: the accepted state and the PLE table |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV quality test, the expert-cache floor test |\n| `docs/` | the design documents, the outcomes, the dated engineering log and the documentation index |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n| `docs/CAMPAIGN.md` | the append-only engineering log (2,000 lines, 134 entries) |\n| `docs/README.md` | the documentation index |\n| `docs/OUTCOME.md` | the outcome document |\n| `docs/DESIGN.md` | the design document |\n| `",
 "ttft_seconds": 1.2185893720015883,
 "status": "ok",
 "tg_tokens_per_s": 37.75588277437799,
 "decode_seconds_per_token": 0.026485938786700098,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.049940002849326,
  "p95": 32.90339199884329,
  "mean": 26.4859387867001
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
