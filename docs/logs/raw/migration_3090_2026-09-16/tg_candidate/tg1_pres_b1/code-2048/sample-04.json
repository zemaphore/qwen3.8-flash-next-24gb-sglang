{
 "started_utc": "2026-09-16T23:21:03.695802+00:00",
 "wall_seconds": 14.618929256001138,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:21:18.314836+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 52,
  "temperature_c": {
   "min": 78.0,
   "max": 79.0,
   "mean": 78.63461538461539
  },
  "power_w": {
   "min": 263.35,
   "max": 279.46,
   "mean": 270.1376923076923
  },
  "util_pct": {
   "min": 87.0,
   "max": 100.0,
   "mean": 91.40384615384616
  },
  "clock_sm_mhz": {
   "min": 1575.0,
   "max": 1905.0,
   "mean": 1876.4423076923076
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "e5ed0ff4dcda44e566290d8fa078a84b6ed9a0fb0fb7d701633cb55ae1dadccc",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one load covers the whole word.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_k[None, :] * stride_bsk\n+            + offs_bn[:, None] * stride_bsn\n+        )\n+        zp_ptrs = (\n+            b_zp_ptr\n+            + off_experts * stride_bze\n+            + offs_k[None, :] * stride_bzk\n+            + offs_bn[:, None] * stride_bzn\n+        )\n+    elif use_int4_w4a16 or use_int8_w8a16:\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+\n+        if use_int8_w8a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) * stride_bzk\n+        elif use_int4_w4a16:\n+            block_start = offs_k[0] + (BLOCK_SIZE_K // 2) * 0\n+            b_zp_ptr_start = b_zp_ptr + (block_start // group_size) *",
 "ttft_seconds": 1.0823497909877915,
 "status": "ok",
 "tg_tokens_per_s": 37.749721615812284,
 "decode_seconds_per_token": 0.026490261575362942,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.271422000718303,
  "p95": 33.4344470029464,
  "mean": 26.490261575362943
 },
 "class": "code",
 "requested_input_tokens": 2048
}
