{
 "started_utc": "2026-09-16T23:20:48.493805+00:00",
 "wall_seconds": 15.200487170004635,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:21:03.694350+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 55,
  "temperature_c": {
   "min": 78.0,
   "max": 79.0,
   "mean": 78.2909090909091
  },
  "power_w": {
   "min": 259.73,
   "max": 277.81,
   "mean": 267.9927272727273
  },
  "util_pct": {
   "min": 88.0,
   "max": 100.0,
   "mean": 92.10909090909091
  },
  "clock_sm_mhz": {
   "min": 1650.0,
   "max": 1905.0,
   "mean": 1885.6363636363637
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "9c3b4cae10a35778d7fec772919d2b6d123485cdcb344d377cf4f6fa90f81cff",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 ==+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 ==+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncont+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(e+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K ==+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(B+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 ==+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == 0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == 0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == 0, \"+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == 0, \"BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == 0, \"BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == 0, \"BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == 0, \"BLOCK_SIZE_K must+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == 0, \"BLOCK_SIZE_K must be+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == 0, \"BLOCK_SIZE_K must be a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == 0, \"BLOCK_SIZE_K must be a multiple+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == 0, \"BLOCK_SIZE_K must be a multiple of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == 0, \"BLOCK_SIZE_K must be a multiple of +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == 0, \"BLOCK_SIZE_K must be a multiple of 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scale/zp tensors are int32 word views ([E, N, K//group_size] words, strides in words)\n+        tl.static_assert(group_size % 16 == 0, \"int2 word path needs group_size % 16 == 0\")\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+        )\n+        b_scale = tl.load(b_scale_ptrs)\n+\n+        if use_int2_w2a16 and has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_bn[None, // 16) * stride_bzk\n+                + tl.arange(0, BLOCK_SIZE_K // group_size)[:, None] * stride_bsk\n+            )\n+            b_zp = tl.load(b_zp_ptrs)\n+        else:\n+            b_zp = tl.zeros_like(b_scale)\n+    else:\n+        if use_int2_w2a16:\n+            pass  # unreachable, handled above\n+        # [ncontig] word-load int2 branch\n+        tl.static_assert(even_Ks, \"int2 word path needs K % BLOCK_SIZE_K == 0\")\n+        tl.static_assert(BLOCK_SIZE_K % 16 == 0, \"BLOCK_SIZE_K must be a multiple of 16",
 "ttft_seconds": 1.1062444809940644,
 "status": "ok",
 "tg_tokens_per_s": 36.25607582662249,
 "decode_seconds_per_token": 0.027581583974559915,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.55255499505438,
  "p95": 34.63576399371959,
  "mean": 27.581583974559916
 },
 "class": "code",
 "requested_input_tokens": 2048
}
