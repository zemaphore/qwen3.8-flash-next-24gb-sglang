{
 "started_utc": "2026-09-16T23:20:15.841879+00:00",
 "wall_seconds": 16.931139752006857,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:20:32.773136+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 61,
  "temperature_c": {
   "min": 77.0,
   "max": 78.0,
   "mean": 77.34426229508196
  },
  "power_w": {
   "min": 246.52,
   "max": 272.93,
   "mean": 258.34754098360656
  },
  "util_pct": {
   "min": 88.0,
   "max": 100.0,
   "mean": 91.65573770491804
  },
  "clock_sm_mhz": {
   "min": 1650.0,
   "max": 1905.0,
   "mean": 1893.4426229508197
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "3edc4c34522b28dc95367d490976b20b522d979c6fce2a2835d16b6aa638b98e",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr +=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N),+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None],+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >>+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3 +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(B+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(com+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >>+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+                b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+                b -=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+                b -= b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+                b -= b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+                b -= b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+                b -= b_zp_num+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+                b -= b_zp_num\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+                b -= b_zp_num\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+                b -= b_zp_num\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+                b -= b_zp_num\n+        elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+                b -= b_zp_num\n+        elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+                b -= b_zp_num\n+        elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+                b -= b_zp_num\n+        elif use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u4e2d\u52a0\u5165\u4e00\u4e2a\u672a\u547d\u4e2d\u7684\u9632\u5fa1\u6027\u8865\u4e01\uff0c\u9632\u6b62\u5728\u7b2c\u4e00\u4e2aBLOCK_K\u7684A\u4e2d A\u51fa\u73b0\u610f\u5916\u884c\u4e3a--\u6ca1\u6709\u5c06\u672a\u547d\u4e2d\u7684A\u6e05\u96f6\n+        if not even_Ks:\n+            a = tl.load(\n+                a_ptrs, mask=offs_k[:, None] < K - k, other=0.0\n+            )\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None], other=0.0)\n+        # We also add the accumulator for the expert\n+        if use_int2_w2a16:\n+            b = tl.load(b_ptrs)\n+            b = (b[:, None, :] >> b_shifter) & 3  # [BLOCK_K//16, 16, BLOCK_N]\n+            b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            b = b.to(compute_type)\n+            if has_zp:\n+                b -= (\n+                    tl.load(b_zp_ptr + b_zp_src_off + (k // group_size) * stride_bzk)[\n+                        None, :, None\n+                    ]\n+                    >> b_zp_shifter\n+                ) & 3\n+            else:\n+                b -= b_zp_num\n+        elif use_int4_w",
 "ttft_seconds": 1.3620033680053893,
 "status": "ok",
 "tg_tokens_per_s": 32.821474900256185,
 "decode_seconds_per_token": 0.030467856884523935,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 30.053584996494465,
  "p95": 38.40875100286212,
  "mean": 30.467856884523936
 },
 "class": "code",
 "requested_input_tokens": 2048
}
