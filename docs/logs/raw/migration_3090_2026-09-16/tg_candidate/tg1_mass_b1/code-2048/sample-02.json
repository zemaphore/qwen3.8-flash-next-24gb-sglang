{
 "started_utc": "2026-09-17T00:09:22.465928+00:00",
 "wall_seconds": 28.0200648750033,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:09:50.486248+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 96,
  "temperature_c": {
   "min": 75.0,
   "max": 77.0,
   "mean": 75.60416666666667
  },
  "power_w": {
   "min": 204.76,
   "max": 261.91,
   "mean": 222.84
  },
  "util_pct": {
   "min": 67.0,
   "max": 100.0,
   "mean": 90.38541666666667
  },
  "clock_sm_mhz": {
   "min": 1635.0,
   "max": 1905.0,
   "mean": 1896.875
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "e7897aad5eb810ac00d729a121639a388d3b1ec606d006e3fe2d5e2d951efec4",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 6+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncont+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # cont+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contigu+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interle+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving).+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e /+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (str+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values);+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N),+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a16 and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a16 and not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a16 and not use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a16 and not use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a16 and not use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a16 and not use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a16 and not use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a16 and not use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a16 and not use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a16 and not use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a16 and not use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    elif use_int2_w2a16:\n+        # [ncontig] scale/zp layout: per (e, n) row, K//group_size values packed\n+        # contiguously (no interleaving). The scale row for expert e / output n\n+        # starts at b_scale_ptr + off_experts * stride_bse + offs_bn * stride_bsn\n+        # (strides in values); the k-th value of the row is at k // group_size.\n+        # Each BLOCK_SIZE_K chunk covers BLOCK_SIZE_K // group_size scale values,\n+        # broadcast over the 16 sub-values of each word.\n+        b_scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_bn[None, :] * stride_bsn\n+            + (offs_k[:, None] // group_size) * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K // group_size\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            if use_int2_w2a16:\n+                b_words = tl.load(b_ptrs)\n+            elif not use_int4_w4a16 and not use_int8_w8a16:\n",
 "ttft_seconds": 1.5365526450041216,
 "status": "ok",
 "tg_tokens_per_s": 19.2951457192025,
 "decode_seconds_per_token": 0.05182650675733438,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 50.218863994814456,
  "p95": 80.39671799633652,
  "mean": 51.82650675733438
 },
 "class": "code",
 "requested_input_tokens": 2048
}
