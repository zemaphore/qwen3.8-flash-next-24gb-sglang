{
 "started_utc": "2026-09-17T00:13:19.709641+00:00",
 "wall_seconds": 31.581161906011403,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:13:51.290972+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 113,
  "temperature_c": {
   "min": 77.0,
   "max": 79.0,
   "mean": 78.53097345132744
  },
  "power_w": {
   "min": 234.88,
   "max": 280.01,
   "mean": 258.84716814159293
  },
  "util_pct": {
   "min": 42.0,
   "max": 99.0,
   "mean": 92.40707964601769
  },
  "clock_sm_mhz": {
   "min": 1320.0,
   "max": 1905.0,
   "mean": 1740.2654867256638
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1496.0
  }
 },
 "output_text_sha256": "c3a8d14ee661bc43e5bb635329ac24238d06790a8327d7f8ccc2c2f96a9e5caa",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    def _aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    def _autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = min(cur + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                self.set_S(cur)\n+                logger.warning(\"elastic: fill to %d MB blocked at S=%d\", reserve_mb, cur)\n+                return\n+            cur = S\n+        logger.info(\"elastic: fill to %d MB free in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            S = max(cur - step, self.s_floor())\n+            self.set_S(S)\n+            cur = S\n+            if cur == self.s_floor():\n+                logger.warning(\"elastic: free to %d MB blocked at S_floor=%d\", reserve_mb, cur)\n+                return\n+        logger.info(\"elastic: free to %d MB in %.2fs (S=%d)\", reserve_mb, time.time() - t0, cur)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, if the reserve is free again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    def _autofill",
 "ttft_seconds": 13.25106032199983,
 "status": "ok",
 "tg_tokens_per_s": 27.877747268843905,
 "decode_seconds_per_token": 0.03587090414287518,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 35.9300499985693,
  "p95": 45.45516399957705,
  "mean": 35.870904142875176
 },
 "class": "code",
 "requested_input_tokens": 32768
}
