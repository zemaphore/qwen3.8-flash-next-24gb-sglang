{
 "started_utc": "2026-09-17T00:12:16.466656+00:00",
 "wall_seconds": 30.99607919700793,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:12:47.462980+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 112,
  "temperature_c": {
   "min": 76.0,
   "max": 79.0,
   "mean": 78.30357142857143
  },
  "power_w": {
   "min": 234.32,
   "max": 279.85,
   "mean": 260.0963392857143
  },
  "util_pct": {
   "min": 49.0,
   "max": 99.0,
   "mean": 93.41964285714286
  },
  "clock_sm_mhz": {
   "min": 1275.0,
   "max": 1905.0,
   "mean": 1748.9732142857142
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1496.0
  }
 },
 "output_text_sha256": "4c34cd65dcc5fb357893c87b95a6b960090af1ffad63befa509bfa942272f1f7",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is None+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is None:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is None:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is None:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is None:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is None:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is None:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is None:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is None:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is None:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is None:\n+            return\n+        free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is None:\n+            return\n+        free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is None:\n+            return\n+        free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free <= self.regrow_reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        S_next = min(S + 8, self.E)\n+        try:\n+            self.set_S(S_next)\n+        except Exception:\n+            self.pending_regrow = False\n+            return\n+        if S_next == self.E:\n+            self.pending_regrow = False\n+\n+    def _autofill(self_step):\n+        \"\"\"Grow toward the autofill target when VRAM is free enough (step-sized).\"\"\"\n+        if self._autofill is None:\n+            return\n+        free = torch.cuda.mem",
 "ttft_seconds": 13.254202028008876,
 "status": "ok",
 "tg_tokens_per_s": 28.8020461231465,
 "decode_seconds_per_token": 0.0347197555244646,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 34.71605099912267,
  "p95": 43.909159998293035,
  "mean": 34.7197555244646
 },
 "class": "code",
 "requested_input_tokens": 32768
}
