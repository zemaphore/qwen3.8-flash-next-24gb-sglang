{
 "started_utc": "2026-09-17T00:11:44.650873+00:00",
 "wall_seconds": 31.811078611994162,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:12:16.462124+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 114,
  "temperature_c": {
   "min": 76.0,
   "max": 79.0,
   "mean": 77.84210526315789
  },
  "power_w": {
   "min": 239.22,
   "max": 279.92,
   "mean": 256.7871052631579
  },
  "util_pct": {
   "min": 68.0,
   "max": 99.0,
   "mean": 94.05263157894737
  },
  "clock_sm_mhz": {
   "min": 1185.0,
   "max": 1920.0,
   "mean": 1748.5526315789473
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1526.0
  }
 },
 "output_text_sha256": "1a2d189459df040031b44ae5b03a4a99b2ceb1ca85baae27556d5b13dde6558f",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (Runtime+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (Runtime+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_next)\n+            except (RuntimeError, ArenaOOM):\n+                torch.cuda.empty_cache()\n+                step = max(1, step // 2)\n+                if step == 1:\n+                    logger.warning(\"elastic free: cannot reach %d MB free at S=%d\", reserve_mb, S)\n+                    return\n+                continue\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        if torch.cuda.mem_get_info()[0] >= reserve:\n+            self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch",
 "ttft_seconds": 13.307465527992463,
 "status": "ok",
 "tg_tokens_per_s": 27.61634136073346,
 "decode_seconds_per_token": 0.03621044463992101,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 36.563539993949234,
  "p95": 44.9234970001271,
  "mean": 36.210444639921015
 },
 "class": "code",
 "requested_input_tokens": 32768
}
