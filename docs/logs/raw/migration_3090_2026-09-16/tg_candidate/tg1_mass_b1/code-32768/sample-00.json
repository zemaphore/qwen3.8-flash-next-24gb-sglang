{
 "started_utc": "2026-09-17T00:11:06.787525+00:00",
 "wall_seconds": 37.8582244769932,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:11:44.645910+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 136,
  "temperature_c": {
   "min": 73.0,
   "max": 78.0,
   "mean": 76.41176470588235
  },
  "power_w": {
   "min": 163.29,
   "max": 283.17,
   "mean": 242.27632352941177
  },
  "util_pct": {
   "min": 0.0,
   "max": 99.0,
   "mean": 82.80147058823529
  },
  "clock_sm_mhz": {
   "min": 1065.0,
   "max": 1920.0,
   "mean": 1773.5294117647059
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1390.0
  }
 },
 "output_text_sha256": "1f310e4408a4741070025931d45b93234738a104061400ea505b8a2f7479e344",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-me+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 10+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 102+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 10+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 102+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                return\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic fill(%d MB): VRAM free %.2f GB at S=%d\", reserve_mb, free / 1024, self.E)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        logger.warning(\"elastic free(%d MB): VRAM free %.2f GB at S_floor=%d\", reserve_mb, free / 1024, self.s_floor())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", f\"s_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena =",
 "ttft_seconds": 19.17177943700517,
 "status": "ok",
 "tg_tokens_per_s": 27.346202742808774,
 "decode_seconds_per_token": 0.03656814839723844,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 36.4840109978104,
  "p95": 45.480237997253425,
  "mean": 36.56814839723844
 },
 "class": "code",
 "requested_input_tokens": 32768
}
