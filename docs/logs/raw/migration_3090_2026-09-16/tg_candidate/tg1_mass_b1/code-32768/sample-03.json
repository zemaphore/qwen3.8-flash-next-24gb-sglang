{
 "started_utc": "2026-09-17T00:12:47.485040+00:00",
 "wall_seconds": 32.20458617499389,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:13:19.689773+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 116,
  "temperature_c": {
   "min": 77.0,
   "max": 79.0,
   "mean": 78.37931034482759
  },
  "power_w": {
   "min": 231.34,
   "max": 280.99,
   "mean": 255.94956896551724
  },
  "util_pct": {
   "min": 64.0,
   "max": 99.0,
   "mean": 93.69827586206897
  },
  "clock_sm_mhz": {
   "min": 1125.0,
   "max": 1905.0,
   "mean": 1757.0689655172414
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1526.0
  }
 },
 "output_text_sha256": "2fca25d4c875a2e3199e26d5918d9389967a0d6102f6d57c625acb84fdcff610",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S +=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    #+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    # ----------------------------------------------------------------+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    # ------------------------------------------------------------------+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    # ------------------------------------------------------------------ control+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    # ------------------------------------------------------------------ control\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    # ------------------------------------------------------------------ control\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    # ------------------------------------------------------------------ control\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    # ------------------------------------------------------------------ control\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized ensure_rows,\n+        one empty_cache per step).\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] < target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            S += step\n+            torch.cuda.empty_cache()\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                logger.info(\"elastic fill: arena ceiling at S=%d (VRAM free %.2f GB)\", S - step,\n+                            torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+            if torch.cuda.mem_get_info()[0] <= target:\n+                logger.info(\"elastic fill: reached S=%d with %.2f GB free\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+                return\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(reserve_mb) << 20\n+        if torch.cuda.mem_get_info()[0] >= target:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < target and S > self.s_floor():\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill target (one-time, on the next call).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        self.fill(self.regrow_reserve)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):",
 "ttft_seconds": 13.408621825001319,
 "status": "ok",
 "tg_tokens_per_s": 27.18680737229546,
 "decode_seconds_per_token": 0.03678254626613655,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 37.371911996160634,
  "p95": 46.7582559940638,
  "mean": 36.78254626613655
 },
 "class": "code",
 "requested_input_tokens": 32768
}
