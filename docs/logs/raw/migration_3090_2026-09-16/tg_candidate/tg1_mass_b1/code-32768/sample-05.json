{
 "started_utc": "2026-09-17T00:13:51.294230+00:00",
 "wall_seconds": 32.72459084300499,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:14:24.018901+00:00",
 "warmup": false,
 "sample_index": 5,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 117,
  "temperature_c": {
   "min": 78.0,
   "max": 79.0,
   "mean": 78.35042735042735
  },
  "power_w": {
   "min": 235.5,
   "max": 282.02,
   "mean": 256.0141025641026
  },
  "util_pct": {
   "min": 68.0,
   "max": 99.0,
   "mean": 94.82905982905983
  },
  "clock_sm_mhz": {
   "min": 1215.0,
   "max": 1905.0,
   "mean": 1751.025641025641
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1526.0
  }
 },
 "output_text_sha256": "f1fdc173ac1bd08b40b8827a2083d75bab2a61d873fcbd174d9503ced8f1cbde",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow S+        \"\"\"Grow S until+        \"\"\"Grow S until the+        \"\"\"Grow S until the driver+        \"\"\"Grow S until the driver's+        \"\"\"Grow S until the driver's free+        \"\"\"Grow S until the driver's free VR+        \"\"\"Grow S until the driver's free VRAM+        \"\"\"Grow S until the driver's free VRAM drops+        \"\"\"Grow S until the driver's free VRAM drops to+        \"\"\"Grow S until the driver's free VRAM drops to reserve+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(res+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve >+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <=+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free -+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes *+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].k+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S ++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room //+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except Arena+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S -+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <=+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                   +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VR+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\",+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                   +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >=+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S ++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_reg+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+   +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VR+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) <<+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 2+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >=+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target -+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes *+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].k+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(),+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S -+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need //+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except Arena+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(),+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S -+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <=+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                   +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\",+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+                   +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+                    break+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+                    break\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+                    break\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+                    break\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+                    break\n+                continue+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+                    break\n+                continue\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+                    break\n+                continue\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+                    break\n+                continue\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+                    break\n+                continue\n+            if+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+                    break\n+                continue\n+            if S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+                    break\n+                continue\n+            if S <=+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+                    break\n+                continue\n+            if S <= self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free0 = torch.cuda.mem_get_info()[0]\n+        reserve = int(reserve_mb) << 20\n+        if reserve > free0:\n+            return\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = free - reserve\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = min(self.E, S + max(1, room // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.S0, S - step)\n+                if S <= self.S0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", self.S0)\n+                    break\n+                continue\n+            if S >= self.E:\n+                break\n+            S = min(self.E, S + step)\n+        self.pending_regrow = True\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        target = int(target_mb) << 20\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target:\n+                break\n+            need = target - free\n+            per = sum(k.arena.row_bytes * len(self.layers) for k in self.layers[next(iter(self.layers))].kinds.values())\n+            S = max(self.s_floor(), S - max(1, need // per))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = max(self.s_floor(), S - step)\n+                if S <= self.s_floor():\n+                    logger.info(\"elastic free: cannot reach %d MB (host slots exhausted at S=%d)\", target_mb, S)\n+                    break\n+                continue\n+            if S <= self.s",
 "ttft_seconds": 13.35017489999882,
 "status": "ok",
 "tg_tokens_per_s": 26.375075465559778,
 "decode_seconds_per_token": 0.03791458345989518,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 37.97766398929525,
  "p95": 47.24290700687561,
  "mean": 37.91458345989518
 },
 "class": "code",
 "requested_input_tokens": 32768
}
