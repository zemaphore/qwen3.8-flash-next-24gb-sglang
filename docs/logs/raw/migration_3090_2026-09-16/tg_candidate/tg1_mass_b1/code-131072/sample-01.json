{
 "started_utc": "2026-09-17T00:32:51.217638+00:00",
 "wall_seconds": 98.1022039620002,
 "prompt_tokens": 131072,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:34:29.319945+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 353,
  "temperature_c": {
   "min": 73.0,
   "max": 78.0,
   "mean": 76.26912181303116
  },
  "power_w": {
   "min": 162.15,
   "max": 280.03,
   "mean": 236.73855524079323
  },
  "util_pct": {
   "min": 0.0,
   "max": 100.0,
   "mean": 70.92351274787535
  },
  "clock_sm_mhz": {
   "min": 1050.0,
   "max": 1920.0,
   "mean": 1701.9263456090653
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 736.0
  }
 },
 "output_text_sha256": "8f4850e8058453709384a9138c06e15add65257d42fca84176ee24ef2520e620",
 "output_text": "\"\"   \"    {'\"    {'OVER\"    {'OVERALL\"    {'OVERALL':\"    {'OVERALL':<\"    {'OVERALL':<1\"    {'OVERALL':<14\"    {'OVERALL':<14s\"    {'OVERALL':<14s}\"    {'OVERALL':<14s} max\"    {'OVERALL':<14s} max {\"    {'OVERALL':<14s} max {overall\"    {'OVERALL':<14s} max {overall['\"    {'OVERALL':<14s} max {overall['max\"    {'OVERALL':<14s} max {overall['max']\"    {'OVERALL':<14s} max {overall['max']:.\"    {'OVERALL':<14s} max {overall['max']:.6\"    {'OVERALL':<14s} max {overall['max']:.6f\"    {'OVERALL':<14s} max {overall['max']:.6f}\"    {'OVERALL':<14s} max {overall['max']:.6f} \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOG\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPRO\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f}\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOG\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPRO\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_ME\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main()\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() ->\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser =\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\",\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\",\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\",\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\",\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\",\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args =\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode ==\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name =\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload =\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path =\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D /\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parent\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True,\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\" \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key,\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n           \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n               \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:1\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s}\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d}\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n               \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d}\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n               \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d}\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n           \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode ==\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name,\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name =\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0],\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a =\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D /\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b =\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D /\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name}\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\",\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a,\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name}\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\",\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b,\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name =\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference =\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D /\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current =\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(c\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report =\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference,\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.qu\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall =\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOG\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPRO\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f}\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOG\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPRO\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_ME\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n       \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name}\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\",\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\nif\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\nif __\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\nif __name\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\nif __name__\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\nif __name__ ==\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\nif __name__ == \"__\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\nif __name__ == \"__main\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\nif __name__ == \"__main__\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\nif __name__ == \"__main__\":\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\nif __name__ == \"__main__\":\n\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\nif __name__ == \"__main__\":\n   \"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\nif __name__ == \"__main__\":\n    main\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\nif __name__ == \"__main__\":\n    main()\"    {'OVERALL':<14s} max {overall['max']:.6f}  mean {overall['mean']:.6f}\")\n    print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--quiet\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        path = D / f\"{name}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(json.dumps(payload))\n        print(f\"  saved {path}\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} tokens={row['forced_tokens']}\"\n            )\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(cached=reference)\n    report = stats(reference, current)\n    if args.quiet:\n        overall = report[\"_overall\"]\n        print(f\"LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n    else:\n        print_report(f\"{name} vs current\", report)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n",
 "ttft_seconds": 78.6641349040001,
 "status": "ok",
 "tg_tokens_per_s": 26.288718344435033,
 "decode_seconds_per_token": 0.0380391309647732,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 38.22496099746786,
  "p95": 47.170260004349984,
  "mean": 38.0391309647732
 },
 "class": "code",
 "requested_input_tokens": 131072
}
