{
 "started_utc": "2026-09-17T00:16:57.646086+00:00",
 "wall_seconds": 28.84737166800187,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:17:26.493552+00:00",
 "warmup": false,
 "sample_index": 5,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 103,
  "temperature_c": {
   "min": 78.0,
   "max": 80.0,
   "mean": 79.32038834951456
  },
  "power_w": {
   "min": 250.42,
   "max": 282.32,
   "mean": 268.65912621359223
  },
  "util_pct": {
   "min": 41.0,
   "max": 99.0,
   "mean": 93.1747572815534
  },
  "clock_sm_mhz": {
   "min": 945.0,
   "max": 1905.0,
   "mean": 1715.2427184466019
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1448.0
  }
 },
 "output_text_sha256": "90532e0aa5924f0860748109a322893ca6eed7bd7c5938b7d8ae966f2702be6f",
 "output_text": "okensokens canokens can beokens can be setokens can be set tookens can be set to theokens can be set to the modelokens can be set to the model'sokens can be set to the model's contextokens can be set to the model's context_lengthokens can be set to the model's context_length;okens can be set to the model's context_length; theokens can be set to the model's context_length; the profileokens can be set to the model's context_length; the profiledokens can be set to the model's context_length; the profiled valueokens can be set to the model's context_length; the profiled value staysokens can be set to the model's context_length; the profiled value stays theokens can be set to the model's context_length; the profiled value stays the physicalokens can be set to the model's context_length; the profiled value stays the physical ceilingokens can be set to the model's context_length; the profiled value stays the physical ceiling andokens can be set to the model's context_length; the profiled value stays the physical ceiling and theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocatorokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator'sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacityokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity =okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = minokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requestokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requestedokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested,okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profileokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiledokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled)okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keepsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admissionokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honestokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest.okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Whyokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why itokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it shouldokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should payokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KVokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV poolokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool isokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 2okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KBokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/tokenokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token xokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 3okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 327okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 3276okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 =okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 7okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 76okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MBokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mappedokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped atokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startupokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup;okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; atokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13kokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokensokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the modelokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~3okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~31okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MBokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB,okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, sookens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~4okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~45okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MBokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB canokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can beokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freedokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed forokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for expertsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts.okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. Atokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At Sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=2okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=23okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the coldokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold massokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass dropsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 %okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % ->okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~6okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MBokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/tokenokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token ->okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 msokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/tokenokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token ->okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 %okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decodeokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode.okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idleokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle releaseokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release mattersokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters forokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for longokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefillsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills,okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, whereokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KVokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV growsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows tookens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~6okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~60okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MBokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB andokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and expertsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts mustokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrinkokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink tookens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to makeokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make roomokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\nokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 20okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 202okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-0okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-0okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:0okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 Sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elasticokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILEDokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED tookens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to startokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start orokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or benchokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench ->okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> revertedokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\nokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 20okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 202okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-0okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-0okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:0okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 Sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elasticokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attemptokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILEDokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED atokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startupokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elasticokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic pathokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs aokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a hostokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slotokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot forokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for everyokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rankokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >=okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= Sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S,okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, butokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placementokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patchokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch onlyokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pinsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rowsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows thatokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloaderokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader leftokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left onokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPUokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU.okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. Theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elasticokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patchokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch nowokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pinsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALLokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL hostokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rankokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rowsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows atokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placementokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloaderokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader'sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's ownokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinnedokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensorsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors areokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freedokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed byokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placementokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement passokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass,okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, sookens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the totalokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinnedokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprintokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint isokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchangedokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 56okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 565okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rowsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows atokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at Sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=18okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\nokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 20okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 202okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-0okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-0okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 Sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elasticokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILEDokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED tookens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to startokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start orokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or benchokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench ->okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> revertedokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\nokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 20okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 202okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-0okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-0okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 Sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elasticokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attemptokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILEDokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED atokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startupokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elasticokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic pathokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs aokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a hostokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slotokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot forokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for everyokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rankokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >=okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= Sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S,okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, butokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placementokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patchokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch onlyokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pinsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rowsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows thatokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloaderokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader leftokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left onokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPUokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU.okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. Theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elasticokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patchokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch nowokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pinsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALLokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL hostokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rankokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rowsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows atokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placementokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloaderokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader'sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloadedokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensorsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors areokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freedokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed byokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placementokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement passokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass,okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, sookens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so theokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the totalokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinnedokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprintokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint isokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchangedokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 56okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 565okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rowsokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows atokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at Sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=18okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\nokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 20okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 202okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-0okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-0okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 Sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elasticokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILEDokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED tookens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to startokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start orokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or benchokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench ->okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> revertedokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\nokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 20okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 202okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-0okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-0okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 Sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S1okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elasticokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic:okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPTokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decodeokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 5okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56.okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56.2okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56.2 tokokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56.2 tok/sokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56.2 tok/s,okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56.2 tok/s, preokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56.2 tok/s, prefillokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56.2 tok/s, prefill okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56.2 tok/s, prefill 2okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56.2 tok/s, prefill 23okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56.2 tok/s, prefill 233okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56.2 tok/s, prefill 2335okens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56.2 tok/s, prefill 2335 tokokens can be set to the model's context_length; the profiled value stays the physical ceiling and the allocator's capacity = min(requested, profiled) keeps admission honest. Why it should pay: the KV pool is 24 KB/token x 32768 = 768 MB mapped at startup; at 13k tokens the model needs ~312 MB, so ~456 MB can be freed for experts. At S=230 the cold mass drops ~1 % -> ~60 MB/token -> ~2.4 ms/token -> ~+10 % decode. Idle release matters for long prefills, where the KV grows to ~600 MB and experts must shrink to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's own pinned tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the elastic path needs a host slot for every rank >= S, but the placement patch only pins rows that the offloader left on the GPU. The elastic patch now pins ALL host-rank rows at placement (the offloader's offloaded tensors are freed by the placement pass, so the total pinned footprint is unchanged: 5658 rows at S=184).\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:19 S13_elastic: KEPT  decode 56.2 tok/s, prefill 2335 tok/s",
 "ttft_seconds": 13.233869571005926,
 "status": "ok",
 "tg_tokens_per_s": 32.72826447765238,
 "decode_seconds_per_token": 0.030554629643830433,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 29.590669000754133,
  "p95": 40.53450499486644,
  "mean": 30.554629643830435
 },
 "class": "prose",
 "requested_input_tokens": 32768
}
