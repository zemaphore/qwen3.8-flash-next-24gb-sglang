{
 "started_utc": "2026-09-17T00:06:07.860949+00:00",
 "wall_seconds": 17.122799774006126,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:06:24.983822+00:00",
 "warmup": false,
 "sample_index": 5,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 60,
  "temperature_c": {
   "min": 75.0,
   "max": 77.0,
   "mean": 76.36666666666666
  },
  "power_w": {
   "min": 230.39,
   "max": 275.14,
   "mean": 256.47316666666666
  },
  "util_pct": {
   "min": 83.0,
   "max": 100.0,
   "mean": 88.8
  },
  "clock_sm_mhz": {
   "min": 1650.0,
   "max": 1920.0,
   "mean": 1893.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "d882ab3ed89d98edbf671eb8a2691bf02fffa08edfc1b0ee272e771002584e74",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`);.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncont.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,04.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert G.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense G.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` must be the directory\ncontaining `ple.json` and `ple.f8_e4m3.bin`. The `--ple-*` flags are the tuned values from CAMPAIGN.md:448.\n`--ple-norm-eps` is not a flag: it is `ple.json`'s `norm_eps` (1e-5), read by the loader.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,040 added lines) and the 10 layered patch scripts it was built from |\n| `kernels/` | the Triton kernels: the int2 expert GEMM, the int8 dense GEMM, the int4 KV pool, the int8 KV ring |\n| `patches/` | the runtime patches: the KV cache tiers, the elastic expert cache, the PLE loader |\n| `assets/` | the two serving assets: the accepted state and the PLE n-gram table |\n| `tools/` | the measurement tools: the benchmark, the KV-cache quality test, the expert-cache controller |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `scripts/` | the launcher and the build scripts |\n| `tests/` | the test suite |\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is",
 "ttft_seconds": 1.367674719003844,
 "status": "ok",
 "tg_tokens_per_s": 32.43400743415885,
 "decode_seconds_per_token": 0.03083183606065342,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 29.164653999032453,
  "p95": 45.734020008239895,
  "mean": 30.831836060653423
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
