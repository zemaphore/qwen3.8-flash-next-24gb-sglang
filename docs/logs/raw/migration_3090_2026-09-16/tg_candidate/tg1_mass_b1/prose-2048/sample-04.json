{
 "started_utc": "2026-09-17T00:05:49.568161+00:00",
 "wall_seconds": 18.290869528995245,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:06:07.859098+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 64,
  "temperature_c": {
   "min": 74.0,
   "max": 76.0,
   "mean": 75.46875
  },
  "power_w": {
   "min": 225.35,
   "max": 268.5,
   "mean": 248.8265625
  },
  "util_pct": {
   "min": 77.0,
   "max": 100.0,
   "mean": 89.25
  },
  "clock_sm_mhz": {
   "min": 1755.0,
   "max": 1920.0,
   "mean": 1911.09375
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "b33b68e68af85c167bfcdd2eed4870291e505b4768597c2a6557ab0a64af07a4",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:32.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:32.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert G.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (through.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f` (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f` (1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f` (1,0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f` (1,04.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f` (1,040.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f` (1,040 added.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f` (1,040 added lines.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f` (1,040 added lines,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f` (1,040 added lines, .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f` (1,040 added lines, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f` (1,040 added lines, 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f` (1,040 added lines, 10 removed.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe: `PLE` must be a path on an NVMe mount, not a tmpfs (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV cache, PLE gather, PLE n-gram lookup)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality gate),\n              ngram_ple.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py (PLE quality gate for the\n              n-gram PLE), ngram_ple_quality.py (PLE quality gate for the n-gram PLE), ngram_ple_quality.py\ndocs/         CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table's schema (ple.json), the PLE table's\n              schema (ple.json), the PLE table's schema (ple.json), the PLE table's schema (ple.json)\n```\n\n## What is in the patch\n\nThe patch is 15 files against SGLang at `73a255206f` (1,040 added lines, 10 removed).",
 "ttft_seconds": 1.3271370559959905,
 "status": "ok",
 "tg_tokens_per_s": 30.123203754809197,
 "decode_seconds_per_token": 0.03319700016437824,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 31.072033001692034,
  "p95": 50.73114100378007,
  "mean": 33.19700016437824
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
