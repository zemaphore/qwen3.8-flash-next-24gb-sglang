{
 "started_utc": "2026-09-17T00:04:51.413973+00:00",
 "wall_seconds": 17.14291617499839,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:05:08.557045+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 61,
  "temperature_c": {
   "min": 72.0,
   "max": 74.0,
   "mean": 73.36065573770492
  },
  "power_w": {
   "min": 213.7,
   "max": 266.2,
   "mean": 253.477868852459
  },
  "util_pct": {
   "min": 78.0,
   "max": 98.0,
   "mean": 88.31147540983606
  },
  "clock_sm_mhz": {
   "min": 1755.0,
   "max": 1920.0,
   "mean": 1907.704918032787
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "ae7b1e2d21ab5af6efb456cc4676e122ff8364061c24e13f26f394498ed4438b",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nS.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.s.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `....py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `....py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144):.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shr.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VR.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_258k.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_258k.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_258k.sh, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_258k.sh, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_258k.sh, ngram_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_258k.sh, ngram_quality_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_258k.sh, ngram_quality_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_258k.sh, ngram_quality_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_258k.sh, ngram_quality_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_258k.sh, ngram_quality_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at load time).\n`--ple-ngram-path` is a path to the table file, not a directory: `PLE` must be `.../ple/ple` (the file\nwithout `.f8_e4m3.bin`), not `.../ple`. The table is served from a pinned host buffer (the `ple` patch)\nand needs 51.2 GB of host RAM; the host RAM floor is 32 GB.\n\nFor a smaller context window, add `--context-window <N>` (a multiple of 128, at most 262,144): it shrinks\nthe KV cache and the ring, and the arena grows to fill the freed VRAM. The default is the full 262,144-token\nwindow.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches, applied in order, that make up the serving patch\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nassets/         the two serving assets: phase1_state.json (the accepted state) and ple.json (the PLE table manifest)\ntools/          the measurement tools: bench.py, ngram.py, ngram_quality.py, ngram_quality.sh,\n                ngram_quality_258k.py, ngram_quality_258k.sh, ngram_quality_258k_",
 "ttft_seconds": 1.4133538729947759,
 "status": "ok",
 "tg_tokens_per_s": 32.48684549137278,
 "decode_seconds_per_token": 0.03078168978473334,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 29.09930700843688,
  "p95": 42.70755899779033,
  "mean": 30.781689784733338
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
